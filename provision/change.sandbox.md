> **AI-assisted change proposal — requested over chat.**
> Requested by `[chat display name]` (`[platform]` user `[stable user id]`) in `[channel]`.
> Filed by a GDD sandbox agent using the `__GITHUB_USER__` machine account, which
> authored the commits but did not decide on the change.
> The requester is a **chat identity, not a GitHub user** — written as plain text
> on purpose, since the same name on GitHub may belong to someone unrelated.

## Summary

- [What this does and why — one bullet per logical change]

## What was asked for

> [The request in the requester's own words, quoted verbatim]

## Test plan

- [ ] [How to verify the change works — link the preview if one was built]

## Related

- Closes #N (if applicable, otherwise remove this line)
